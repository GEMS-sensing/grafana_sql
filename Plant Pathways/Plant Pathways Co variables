## PepsiCo homepage variables

Data Sources
PostgreSQL Sensing0 PPC

variables linked to dashboard
$node
query : select distinct(display_name, node_id) from $project_key.meta where project = 'PlantPathways';
regex : /\((?<text>[^,]+),(?<value>[^)]+)\)/

# project_key
plantpathways