## Roadside Turf variables

Data Sources
PostgreSQL Sensing0 Ingest

variables linked to dashboard
$node
query : select distinct(display_name, node_id) from $project_key.meta where project = 'Roadside Turf'
regex : /\((?<text>[^,]+),(?<value>[^)]+)\)/

# project_key
roadsideturf